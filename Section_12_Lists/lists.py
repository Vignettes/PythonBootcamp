#Lists are ways of holding data

a = [1, 2, 3]

#You can use function like len() to check length

len(a) #would return 3

#You can build a list from a range
r = range(1,10)
list(r)

#also done as
nums = list(range(1,100)) #creates a list with the range 1-99